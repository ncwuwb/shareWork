# 基于Java的共享工位管理系统 - 需求规格说明书 (SRS)

## 1. 引言

### 1.1 编写目的

本文档旨在详细描述“基于Java的共享工位管理系统”的功能需求、非功能需求以及业务流程，为开发团队提供清晰的系统设计和开发指南，同时作为项目验收的标准依据。

### 1.2 项目背景

随着混合办公模式（Hybrid Work）的普及和企业对办公空间利用率要求的提升，传统的固定工位已不能满足现代企业的灵活需求。本系统旨在通过数字化手段，实现工位的共享化、预约化、可视化管理，从而优化空间资源配置，降低企业运营成本，提升员工办公体验。

### 1.3 适用范围

本系统既适用于企业内部的灵活办公区域管理，也适用于对外出租的联合办公空间（Co-working Space）。

## 2. 总体描述

### 2.1 用户角色定义

系统主要包含以下两类用户角色：

1. **普通用户（员工/租客）**：系统的实际使用者，主要进行工位的查询、预约、签到、退订等操作。
2. **系统管理员（行政/运营人员）**：负责基础数据维护、工位管理、订单管理、数据统计分析及系统设置。

### 2.2 确定技术栈

- **后端**：Java 17+, Spring Boot 3.x, Spring Security (权限控制), WebSocket (消息实时推送)
- **持久层**：MyBatis-Plus, MySQL 8.0 (主业务数据)
- **缓存与并发控制**：Redis (用于处理高并发预约防超卖、Token缓存、GEO地理位置签到计算)
- **前端 (后台管理)**：Vue 3 + Element Plus (提供流畅的单页应用体验与丰富的后台组件)
- **移动端 (用户端)**：Uni-app (一套代码多端发布，重点适配微信小程序端，方便用户随时随地预约和进行位置签到)

## 3. 功能性需求（详细设计）

### 3.1 移动端/用户端模块（普通用户 - Uni-app实现）

用户端注重交互体验和便捷性，基于Uni-app实现多端适配（以微信小程序为主）。

| 功能模块       | 功能子项   | 详细功能说明与约束                                           |
| -------------- | ---------- | ------------------------------------------------------------ |
| **用户认证**   | 注册与登录 | 1. 仅使用手机号及验证码进行登录。2. **验证码下发**：暂时不接入真实的短信发送网关，验证码由后端生成并直接在控制台（Console）打印输出供测试使用。3. 登录成功后发放JWT Token，有效期建议设置为7天。 |
|                | 个人中心   | 1. 展示用户基本信息（头像、姓名、部门）。2. **信用积分模块**：展示当前积分，提供积分变更明细列表（如：违约扣10分，按时签到加1分）。3. 常见问题解答(FAQ)与意见反馈入口。 |
| **工位大厅**   | 空间可视化 | 1. **层级联动**：支持“楼宇 -> 楼层 -> 区域”的三级下拉联动切换。2. **地图交互**：提供2D平面图，支持双指缩放、拖拽。点击工位图标可弹出工位详情弹窗（包含实景图、编号、设施）。3. **状态图例**：实时渲染工位颜色（绿色-空闲，红色-已预约，黄色-使用中，灰色-不可用）。 |
|                | 筛选与搜索 | 1. **时间维度**：必须先选择期望使用的日期和时间段（支持全天/上午/下午/自定义小时），系统再过滤出空闲工位。2. **设施维度**：支持多选标签（如：双屏、升降桌、靠窗、近电源）。 |
| **预约管理**   | 发起预约   | 1. **防冲突校验**：同一用户在同一时间段内只能预约一个工位。2. **时长限制**：单次预约**最少1小时，最多不超过24小时**。3. **提交二次确认**：弹出确认框，明示预约时间、地点及违约扣分规则。 |
|                | 订单管理   | 1. 分类列表：提供“进行中”、“已完成”、“已取消”、“已违约”四个Tab页。2. 订单详情页：仅用于展示预约的开始/结束时间、楼层、区域、工位编号等相关基础信息。 |
|                | 取消预约   | 1. **无责取消**：预约开始时间前 **1小时** 取消，不扣信用分。2. **有责取消**：距离预约开始不足1小时或开始后取消，需扣除一定信用分，并记录操作日志。 |
| **签到与释放** | 位置签到   | 1. **核心动作**：用户到达办公区后，在移动端点击“签到”按钮。2. **GEO距离校验**：移动端获取当前设备的GPS经纬度，发送至后端。后端使用**Redis GEO**功能计算当前位置与办公区中心点的距离，只要在**半径500米内**即可完成签到成功。3. **时间窗口校验**：仅允许在预约开始前15分钟至开始后30分钟内签到。 |
|                | 主动退座   | 用户提前结束办公时，点击“结束使用”，系统立即释放当前工位，变更状态为“空闲”。 |
| **消息通知**   | 消息触达   | 1. 站内信列表：按时间倒序排列。2. 关键节点推送：预约成功通知、签到倒计时提醒、系统强制释放通知、信用分扣除警告。**采用 WebSocket 技术**实现服务端向客户端的实时消息推送。 |

### 3.2 后台管理端模块（系统管理员 - Vue 3 + Element Plus实现）

管理端需要强大的数据管理和状态干预能力，基于Vue 3构建高效后台。

| 功能模块       | 功能子项       | 详细功能说明与约束                                           |
| -------------- | -------------- | ------------------------------------------------------------ |
| **基础数据**   | 空间层级管理   | 1. 维护“校区/园区 -> 楼宇 -> 楼层 -> 办公区域”的树状字典结构。2. 配置办公区的中心点经纬度（用于计算打卡距离）。3. 支持上传楼层或区域的平面背景底图。 |
|                | 设施标签管理   | 增删改查工位配套设施标签（带图标配置），用于前端筛选。       |
| **工位管理**   | 工位台账维护   | 1. **批量生成**：指定区域和前缀，一键生成连续编号的工位（如 A-001 到 A-050）。2. **坐标绑定**：在上传的平面底图上，拖拽标记工位的X/Y坐标点。3. 支持单个工位信息的修改（绑定特定设施）。 |
|                | 状态强制干预   | 1. **维修锁定**：将报修工位状态设为“维护中”，移动端将不可见或不可预约。2. **专属保留**：将工位分配给固定人员（如部门主管），该工位不再参与公共流转。 |
| **预约与订单** | 预约看板       | 1. **时间轴视图**：以甘特图形式横向展示各工位在当天的占用时间段，支持横向拖动时间轴。2. 管理员可在此界面点击任意预约块，查看预约人详情或执行强制取消。 |
|                | 异常订单处理   | 查询所有“已违约”状态的订单，支持用户线下申诉后，管理员在此处进行“撤销违约并恢复积分”的操作。 |
| **用户管理**   | 组织架构与员工 | 1. 支持Excel模板批量导入/导出员工名单。2. 维护员工所属部门、职位及联系方式。3. 账号封禁与解封操作。 |
|                | 信用体系管控   | 1. **全局规则设置**：设置初始分（如100分），迟到/未签到扣减分值，规范释放奖励分值。2. **惩罚阈值设置**：当用户积分低于X分时（如60分），系统自动限制其未来Y天内（如7天）无法提交新的预约。 |
| **数据看板**   | 多维统计报表   | 1. **实时数据**：今日预约总数、当前签到率、空置率。2. **图表渲染**：**统一使用 ECharts** 渲染各类分析图表，包括：过去30天最受欢迎的区域Top3、最抢手的工位Top10等。3. **违约统计**：各部门违规率排行柱状图，辅助行政人员进行通报。支持报表导出(Excel)。 |
| **系统设置**   | 权限与角色     | 1. 角色极简化：系统仅内置 **“超级管理员”**（拥有所有权限）和 **“区域管理员”**（仅能管理分配给自己的特定楼层/区域的数据）两种角色。2. 精确到按钮级别的权限分配。 |
|                | 全局业务参数   | 配置预约规则参数，包括：可提前预约的最长天数、允许签到的时间窗口（如前后30分钟）、GEO签到有效半径（默认500米）等。 |

## 4. 业务流程说明

本系统最核心的三个业务流程详细流转步骤如下，开发人员需重点关注其中的并发处理与异常判断逻辑。

### 4.1 核心预约流程（含并发防超卖控制）

此流程发生在Uni-app使用端，是系统并发压力最大的环节（如早上固定时间抢座）。

- **前置条件**：用户已登录，且当前信用积分 >= 系统规定的最低预约门槛。
- **处理步骤**：
  1. **提交请求**：用户选择 [工位A] 和 [时间段 T1-T2]，发起预约请求。
  2. **业务基础校验**：
     - 检查该用户在 T1-T2 时间段内是否已有其他预约（防分身）。
     - 检查预约时长是否在 **1小时至24小时** 的区间内。
  3. **并发加锁校验（关键节点）**：
     - 系统根据 `工位ID_日期_时间段` 生成Redis锁的Key。
     - 尝试获取Redis分布式锁（使用 `setnx` 或 Redisson）。
     - **异常处理**：若获取锁失败，说明该工位正被他人抢占，立即向用户返回提示：“该工位已被抢先预约，请刷新后重新选择”。
  4. **数据库落盘**：
     - 获取锁成功后，开启数据库事务。
     - 再次查询数据库中该工位在此时间段的状态，确保绝对无冲突。
     - 生成预约订单记录（状态为：待签到）。
     - 提交事务。
  5. **释放锁与通知**：释放Redis锁，向用户返回成功响应，并通过 **WebSocket** 触发异步消息推送（预约成功通知）。

### 4.2 签到与防占座流程（基于Redis GEO状态流转）

此流程涉及用户移动端发起位置定位及后端的定时任务监控，用于解决“占着位置不来”的痛点。

- **前置条件**：用户拥有一个状态为“待签到”的预约订单。
- **处理步骤 (用户正常签到分支)**：
  1. 用户到达办公区周边，在Uni-app端点击订单的“签到”按钮。
  2. 前端获取当前设备的GPS经纬度并提交给后端。
  3. 后端接收经纬度，并校验：
     - **时间窗口校验**：当前时间是否在 [预约开始时间 - 15分钟，预约开始时间 + 30分钟] 的允许窗口内。**异常处理**：未到时间提示“签到时间未到”，超时提示“已过签到时间，订单已作废”。
     - **距离校验**：利用 **Redis 的 GEO（地理位置）特性**（如 `GEODIST` 命令），计算用户当前位置与系统设置的办公区中心点坐标的直线距离。若距离 **<= 500米**，则判定位置合格。**异常处理**：若大于500米，提示“您当前不在办公区域范围内，签到失败”。
  4. 校验全部通过，更新订单状态为“进行中”（已签到），工位实时状态变为“使用中”。
- **处理步骤 (定时任务违约分支)**：
  1. 后端定时任务（如使用 Spring Task 或 Quartz）每隔1分钟轮询一次数据库中状态为“待签到”的订单。
  2. 识别出 **当前时间 > 预约开始时间 + 30分钟（系统配置的宽限时间）** 的订单。
  3. 执行自动化处理：
     - 将订单状态强制变更为“已违约”。
     - 将对应的工位状态重新释放为“空闲”。
     - 扣除预约用户的信用积分，并记录积分变更流水。
     - 通过 **WebSocket** 下发“违约扣分与工位释放”通知给用户。

### 4.3 提前退订与释放流程

用于提高工位的周转率，鼓励员工在提前完成工作后让出空间。

- **处理步骤 (未开始时的退订)**：
  1. 用户在预约开始前进入订单点击“取消预约”。
  2. 系统计算当前时间距离预约开始时间的差值。
  3. 若大于无责取消时间（**提前1小时以上**），直接取消订单，释放工位。
  4. 若小于无责取消时间（**不足1小时**），弹出提示框“当前取消将扣除X分信用分，是否继续？”。用户确认后，执行取消、释放工位并扣分。
- **处理步骤 (使用中的提前释放)**：
  1. 用户在签到后，若实际离开时间早于预约结束时间，点击“结束使用”。
  2. 系统更新订单状态为“已完成”，记录实际结束时间。
  3. 立即将该工位状态重置为“空闲”，允许其他用户立即预订该工位的剩余时间段。
  4. （可选）对于按规范主动提前释放的用户，系统可奖励少许信用积分以作正向激励。

## 5. 非功能性需求

### 5.1 性能需求

- **响应时间**：常规查询操作响应时间需在 **1秒** 以内；预约提交等事务性操作响应时间需在 **2秒** 以内。
- **并发处理**：系统需能够支撑早高峰期集中预约和打卡的并发请求，设计上需引入Redis缓存工位状态，减轻数据库瞬时压力。

### 5.2 安全性需求

- **接口安全**：所有API请求必须携带有效Token（JWT），并进行权限校验。
- **防恶意刷单**：对同一个账号的预约频率进行限流（Rate Limiting），防止脚本刷位。
- **数据安全**：用户密码需使用Bcrypt等强加密算法存储；数据库需具备定期自动备份机制。

### 5.3 兼容性与易用性

- **后台(Vue 3)** 需兼容主流现代浏览器（Chrome, Edge, Firefox, Safari）。
- **移动端(Uni-app)** 需做自适应处理，保证在iOS、Android及微信小程序环境下的一致性体验，操作链路应尽量短。